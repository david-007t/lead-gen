# ARCHITECTURE.md — Technical architecture
# Project: [INFERRED] Leadqual
# Version: 1.0
# Last updated: 2026-04-14 by [INFERRED] Codex

---

## Three-layer architecture

### Layer 1 — Data
Source: [INFERRED] User-entered lead data, Supabase records, and AI-generated search results via Anthropic.
Ingestion method: [INFERRED] [INFERRED] Client-side lead state with Supabase-backed persistence and optional CSV import/export.
Pipeline tool: [INFERRED] User-triggered browser workflow with serverless API proxying.
Update schedule: [INFERRED] On-demand user-triggered refresh and editing.
Storage location: [INFERRED] [INFERRED] Supabase.

### Layer 2 — AI orchestration
Model: [INFERRED] Anthropic messages API accessed through `api/anthropic.js`.
Role: [INFERRED] Generate or score leads based on industry filters and qualification criteria.
Input format: [INFERRED] [INFERRED] Browser state, Supabase rows, CSV imports, and Vercel API JSON responses.
Output format: [INFERRED] React UI tables, charts, and exported CSV data.
Prompt approach: [INFERRED] Derived from the current repository implementation and integration paths.

### Layer 3 — Output
Delivery: [INFERRED] Browser dashboard deployed on Vercel.
Format: [INFERRED] React UI tables, charts, and exported CSV data.
How user receives it: [INFERRED] Through the currently visible operator or user-facing surfaces in the repo.

---

## System components

### Frontend
Framework: [INFERRED] React 18 + Vite single-page app.
Key pages: [INFERRED] `src/LeadQualifier.jsx` single-page dashboard and `api/anthropic.js` proxy.
State management: [INFERRED] Component-local React state with Supabase persistence.

### Backend
Framework: [INFERRED] Vercel serverless functions plus Supabase.
Database: [INFERRED] Supabase.
Key tables: [INFERRED] Lead storage is inferred from Supabase client usage in the UI.
RLS strategy: [INFERRED] RLS posture needs verification because auth is not visible in the repo.

### Auth
Provider: [INFERRED] No production auth flow implemented yet.
Session strategy: [INFERRED] No session strategy is implemented in the visible codebase.
Protected routes: [INFERRED] No protected routes are currently visible.

### Integrations
[INFERRED] Anthropic proxy: [INFERRED] Sends model requests server-side so the API key stays off the client. — [INFERRED] `api/anthropic.js` forwards request bodies directly to Anthropic.

---

## Key decisions

Summary of most important decisions (full log in DECISIONS.md):
- [INFERRED] The visible repository shape is treated as the frozen baseline for governance.
- [INFERRED] Review artifacts stay FAIL until there is fresh verification evidence.

---

## Data flow

1. [INFERRED] Source data enters through the current project-specific ingestion path.
2. [INFERRED] The existing backend or pipeline transforms and stores the working data.
3. [INFERRED] Any AI-assisted logic runs where it is already wired in the repository.
4. [INFERRED] The current output surface delivers the result back to the user or operator.

---

## Environment variables

| Variable | Purpose | Required |
|----------|---------|----------|
| [INFERRED] Project-specific env vars already referenced in code | [INFERRED] Data access, auth, and external API integration | [INFERRED] Yes |

---

## Changelog

v1.0 — 2026-04-14 — Initial architecture. Generated by Claude Code.
